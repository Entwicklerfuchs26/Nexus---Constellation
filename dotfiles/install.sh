#!/usr/bin/env bash
# ÜBERFLÜSSIG seit 15.09.2026 -- alles was hier stand (skwd-wall/matugen-
# Runtime-Dateien) läuft jetzt deklarativ über Home-Manager, siehe
# home/skwd-wall.nix. Ein normales `rebuild` reicht.
#
# Bleibt als Datei stehen (statt gelöscht) für den Fall, dass irgendwo noch
# eine alte Anleitung/ein alter Link drauf verweist.
echo "Dieses Skript wird nicht mehr gebraucht -- 'rebuild' reicht, siehe home/skwd-wall.nix."
